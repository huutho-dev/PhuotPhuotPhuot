<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="150dp"
    android:layout_marginLeft="2dp"
    android:layout_marginRight="2dp"
    android:layout_marginTop="2dp">

    <ImageView
        android:id="@+id/layout_item_motel_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />

    <com.huutho.phuotphuotphuot.widget.TextViewRoboto
        android:id="@+id/layout_item_motel_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#9da30404"
        android:ellipsize="end"
        android:lines="1"
        android:padding="@dimen/spacing_0_4dp"
        android:textColor="@color/white"
        android:textSize="@dimen/text_1_10sp"
        android:textStyle="bold"
        app:textFonts="RobotoMedium" />

    <include layout="@layout/layout_loading" />
</RelativeLayout>